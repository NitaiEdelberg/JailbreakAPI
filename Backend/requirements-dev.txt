# Dev/test-only dependencies (not needed to run the API in production).
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest -q            # run from Backend/
-r requirements.txt
pytest
httpx            # required by fastapi.testclient (TestClient)
